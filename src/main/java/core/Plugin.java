package core;

public class Plugin {
    /**
     * Has the plugin been loaded completely into the engine
     */
    private boolean isLoaded;

    /**
     * Is the plugn active in the engine
     */
    private boolean isActive;
}
